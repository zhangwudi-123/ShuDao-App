"use strict";

require("core-js/modules/es.function.name");

require("core-js/modules/es.string.replace");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getPrefixKey = getPrefixKey;

var path = require('path');

var fs = require('fs');

var appDirectory = fs.realpathSync(process.cwd());

var resolveApp = function resolveApp(relativePath) {
  return path.resolve(appDirectory, relativePath);
};

function getPrefixKey(key) {
  // 若 key 没有定义， 使用 package.json 的 name
  if (!key) {
    var pkg = require(resolveApp('package.json'));

    key = pkg.name.replace(/^@hvisions\/(module|client)-/, '');
  }

  return key;
}