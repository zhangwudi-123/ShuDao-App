let router = [];
let blockPage = {};
let store = {};
let init;
let en_US = {};
let zh_CN = {};

try {
  router = require('../router').default;
} catch (e) {
  // do noting
}

try {
  blockPage = require('../blockPage')
} catch (error) {
  //
}

try {
  store = require('../store').default;
} catch (e) {
  // do noting
}
try {
  init = require('../init').default;
} catch (e) {
  // do noting
}

try {
  en_US = require('../locales/en_US');
  zh_CN = require('../locales/zh_CN');
} catch (e) {
  // do noting
}

// if (__webpack_modules__[require.resolveWeak('../router')]) {
//   // Do something when module is available...
//   router = require('../router').default;
// }
// if (__webpack_modules__[require.resolveWeak('../store')]) {
//   // Do something when module is available...
//   store = require('../store').default;
// }

export default { init, router, blockPage, store, locale: { en_US, zh_CN } };

export { default as action } from './action';
export { default as selector } from './selector';
export { default as service } from './service';
